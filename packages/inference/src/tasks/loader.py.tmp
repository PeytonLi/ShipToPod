import type { CodeTask } from "@shiptopod/core";
import { randomUUID } from "node:crypto";

export const EVAL_SPLIT_SIZE = 0.2;

const PYTHON_TASKS: Omit<CodeTask, "id">[] = [
  {
    prompt: "Write a function is_even(n) that returns True if n is even, False otherwise.",
    language: "python",
    hidden_tests: "def test_is_even():
    assert is_even(2) == True
    assert is_even(3) == False",
    source: "mbpp",
  },
  {
    prompt: "Write a function factorial(n) that returns the factorial of n.",
    language: "python",
    hidden_tests: "def test_factorial():
    assert factorial(0) == 1
    assert factorial(5) == 120",
    source: "mbpp",
  },
];
